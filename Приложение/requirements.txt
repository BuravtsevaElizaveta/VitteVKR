# AutoIntellect – deps for Streamlit Cloud (Python 3.13)

# ── UI
streamlit==1.50.0
# если действительно используете камеру/WebRTC — оставьте, иначе удалите следующую строку
streamlit-webrtc==0.47.6

# ── ML (совместимо с Py 3.13)
torch==2.7.0
torchvision==0.22.0
ultralytics==8.3.210

# ── OpenCV (только headless!)
opencv-python-headless==4.9.0.80

# ── Python ecosystem
numpy>=2.1.2
pandas>=2.3.0
sqlalchemy==2.0.18
psutil==5.9.8
pillow>=10.3.0
plotly==5.15.0

# ── OpenAI
openai==1.72.0
