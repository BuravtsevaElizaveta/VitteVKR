# AutoIntellect – unified deps (Python 3.13 friendly)

# ── UI / App ─────────────────────────────────────────────────────────
streamlit>=1.42.2
streamlit-webrtc==0.47.0

# ── ML stack (Py3.13) ────────────────────────────────────────────────
# PyTorch под 3.13: используем современную ветку
torch==2.7.1
torchvision==0.22.0
ultralytics>=8.0.0

# OpenCV: только headless, чтобы избежать конфликтов с системным GUI
opencv-python-headless==4.10.0.84

# ── Python ecosystem ─────────────────────────────────────────────────
# Numpy/Pandas версии, у которых есть колёса под 3.13
numpy>=2.1.2
pandas>=2.3.0
sqlalchemy==2.0.18
psutil==5.9.5
pillow>=10.3.0
plotly==5.15.0

# ── OpenAI via proxy ────────────────────────────────────────────────
openai==1.72.0
